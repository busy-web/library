{
  "name": "@busy-web/data",
  "version": "0.0.1",
  "description": "busy-web data model tools",
  "main": "index.js",
  "author": "busy inc.",
  "license": "MIT",
  "keywords": [
    "busy-web",
    "@busy-web",
    "@busy-web/data"
  ],
  "scripts": {
    "serve": "make serve",
    "build": "make build",
    "test": "make test",
    "setup": "make install"
  },
  "devDependencies": {
    "@types/jest": "^23.1.4",
    "babel-core": "^6.26.3",
    "eslint-plugin-typescript": "^0.12.0",
    "jest": "^23.2.0",
    "jest-runner-tsc": "^1.2.0",
    "ts-jest": "^23.0.0",
    "tslint": "^5.10.0",
    "typescript": "^2.9.2"
  },
  "engines": {
    "node": "^4.5 || 6.* || >= 7.*"
  }
}
